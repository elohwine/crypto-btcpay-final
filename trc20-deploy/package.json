{
  "name": "trc20-test-deploy",
  "version": "1.0.0",
  "description": "Deploy & mint a Test USDT (TRC-20) on Tron testnet (Shasta) using TronWeb + solc",
  "main": "scripts/deploy.js",
  "scripts": {
    "deploy": "node scripts/deploy.js",
    "mint": "node scripts/mint.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "fs-extra": "^11.1.1",
    "solc": "^0.8.20",
    "tronweb": "^6.0.0"
  }
}
