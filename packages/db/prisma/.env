# Local dev Prisma env for @repo/db
# This is used by `pnpm --filter @repo/db exec prisma ...` commands.
# When Postgres runs via Docker with port 5432 published, connect via localhost.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cryptoplatform"
